# Ініціалізація розмірів масиву
rows = 7
cols = 7

# Цикл для створення та виведення матриці
for i in range(rows):
    for j in range(cols):
        # Перевірка на парність/непарність рядка для виводу 1 або 0
        if i % 2 == 0:
            print("1", end="")
        else:
            print("0", end="")
    print()  # Після завершення рядка переходимо на новий
